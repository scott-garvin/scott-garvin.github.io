# Optional Docker Compose configuration. Sample mode works without this file.
APP_MODE=sample
FRONTEND_PORT=3001
POSTGRES_PORT=5433
# Local development only. Use a dedicated database and managed secrets when deploying.
POSTGRES_PASSWORD=local-development-only
# Required only for live mode. Never commit real values.
OPENAI_API_KEY=
OPENAI_MODEL=
API_ACCESS_KEY=
EMBEDDING_MODEL=text-embedding-3-small
