# Exporting project tasks to CSV

Owners and admins can export project tasks as a CSV on every plan. Open a project, select the Tasks view, then choose Export > CSV. The export respects the current filters. To include completed tasks, change the status filter to All before exporting. Files include task names, statuses, assignees, due dates, and completion dates. Attachments and comment threads are not included in a CSV export.
