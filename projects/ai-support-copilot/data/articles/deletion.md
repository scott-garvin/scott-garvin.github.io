# Deleting a workspace

Workspace deletion is an owner-only action that requires confirmation in Settings > Workspace > Delete. Deletion is permanent after the seven-day recovery window. Export needed project data before requesting deletion. The support copilot cannot delete a workspace or confirm deletion on behalf of a user.
